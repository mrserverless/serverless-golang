version: '3'
services:
  go-shim:
    image: yunspace/serverless-go-shim:latest
    env_file: .env
    volumes:
      - ${GOPATH}/src:/go/src
  serverless:
    image: amaysim/serverless:1.14.0
    env_file: .env
    volumes:
      - .:/build
      - ~/.aws:/root/.aws
    working_dir: /build
