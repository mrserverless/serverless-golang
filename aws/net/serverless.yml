service: serverless-golang
package:
  artifact: handler.zip
provider:
  name: aws
  runtime: python2.7
  stage: ${env:ENV}
  region: ${env:AWS_DEFAULT_REGION}
  profile: ${env:AWS_PROFILE}
functions:
  hello-net:
    handler: handler.NetHandle
    events:
      - http: GET hello-net